import { HovershadowDirective } from './hovershadow.directive';

describe('HovershadowDirective', () => {
  it('should create an instance', () => {
    const directive = new HovershadowDirective();
    expect(directive).toBeTruthy();
  });
});
